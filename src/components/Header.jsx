import React from "react";

const Header = () => {
	return (
		<header>
			<h1>Click counter</h1>
			<p>
				A very simple app that counts how many times a user clicks a button. It
				is really that simple.
			</p>
		</header>
	);
};

export default Header;
